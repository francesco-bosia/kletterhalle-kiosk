'use client';

/**
 * Idle timer stub.
 * Will be implemented in a later phase to detect user inactivity
 * and dispatch RESET_SESSION after a timeout.
 */
export function IdleWatcher() {
  return null;
}
